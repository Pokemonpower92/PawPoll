import { React, useEffect } from 'react';
import * as d3 from 'd3';

export default function PollBar(props) {

    const { pollData } = props;

    return (
        <h1> Bar chart for a poll.</h1>
    )
}